export interface Section {
    id:number,
    section:string,
    image:string,
    alt_text:string
}

export interface HeroImage {
    id:number,
    order:number,
    image:string,
    alt_text:string
}